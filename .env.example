# Mail server configuration
IMAP_HOST=imap.example.com
IMAP_PORT=993
SMTP_HOST=smtp.example.com
SMTP_PORT=587

# Optional: override email domain (defaults to IMAP_HOST without "mail." prefix)
# MAIL_DOMAIN=example.com